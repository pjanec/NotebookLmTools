# Secrets — never commit (see CLAUDE.md)
credentials.json
token.json
*.token
*.pem
*.key
.env
.env.*
secrets/
.secrets/
cookies.txt
locks/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Local output
answers/

# rclone: the binary is fetched by setup.ps1; the lock file IS committed so
# another machine installs the same version.
rclone.conf
tools/rclone.exe
tools/*.zip

# Generated test bundles (reproducible from a seed)
tests/fixtures/
